#!/usr/bin/env bash

# A bash script to display "Best School" 10 times using a while loop

# Initialize counter
counter=1

# Use a while loop to iterate until counter reaches 10
while [ $counter -le 10 ]; do
    echo "Best School"
    
    # Increment the counter
    ((counter++))
done
